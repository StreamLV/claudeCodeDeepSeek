# claudeCodeDeepSeek

Build: 
```bash
docker compose build
```

Run claude:
```bash
docker compose run --rm claudecode_deepseek
```

Stop containers:
```bash
docker compose down
```

Rebuild containers:
```bash
docker compose build --no-cache
```